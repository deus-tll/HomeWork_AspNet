я завантажую цей один проект на дві домашки(на 16-ту та 17-ту). можливо ще навіть 18-ту завантажу пізніше, бо в цьому проекті мені довелось використати Сесії, якщо ще додати логування та куки, то вже по суті вона буде виконана.

дз 16:
1.) з приводу бази даних, то в мене декілька таблиць, повідомлення, продукти і ті таблиці які створює IdentityUser. для продуктів (є три операції які вимагаються: видалення, створення та редагування даних).

2.) сортування, фільтрування та пагінація даних реалізовані. там легко знайдете в HomeController на перших діях.
щодо пагінації прийшлось повозитись, щоб синхронізувати роботу цього еханізму і при фільтруванні, але ніби як розібрався


дз 17:
с приводу цієї домашки можу сказати, що в мене в цілому весь проект там за допомогою бутстрапу оформлюється, як і розмітка, як і форми, як і адаптив, додатково зробив таблицю з продуктами яку теж з бутстрапом оформив, пагінацію до речі теж оформив з бутстрапом. а от щодо акордеону чи каруселі, то це вже реалізую в екзаменаційній роботі, там вже для кожного з товарів в мене будуть по декілька картинок і там можно буде застосувати ту ж карусель і т.д.


ПРИМІТКА:
можливо вам доведеться видалити базу яка в вас вже є і запустити заново проект, щоб перестворити її, я вже не пам'ятаю чи вносив якісь зміни.
також це допоможе створити 70 тестових записів в базу за допомогою такого методу в класі ProductInitializer, щоб було більш простіше перевіряти роботу пагінації -

private async Task SeedProductsAsync()
        {
            List<Product> products = await _context.Products.AsNoTracking().ToListAsync();

            if (!products.Any())
            {
                for (int i = 0; i < 7; i++)
                {
                    await _context.AddRangeAsync(CreateListOfDefaultProducts());
                    await _context.SaveChangesAsync();
                }
            }
        }